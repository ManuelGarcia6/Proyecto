# Proyecto Ágape - Avance 3.5

# Constantes del Programa
DIAS_DEL_ANIO = 365
LIMITE_DE_DIAS_SIN_HABLAR = 15

# --------------- Funciones principales ---------------
def dia_del_year(dia, mes):
    """
    (uso de condicionales, uso de funciones)
    recibe: dia (valor numerico entero), mes (valor numerico entero del 1 al 12)
    suma los dias de los meses anteriores al mes dado y le suma el dia
    (se usa un anio de 365 dias, sin bisiestos)
    devuelve: numero de dia del anio entre 1 y 365, (entero)
    """
    if mes == 1:
        acumulado = 0
    elif mes == 2:
        acumulado = 31
    elif mes == 3:
        acumulado = 59
    elif mes == 4:
        acumulado = 90
    elif mes == 5:
        acumulado = 120
    elif mes == 6:
        acumulado = 151
    elif mes == 7:
        acumulado = 181
    elif mes == 8:
        acumulado = 212
    elif mes == 9:
        acumulado = 243
    elif mes == 10:
        acumulado = 273
    elif mes == 11:
        acumulado = 304
    else:
        acumulado = 334
    return acumulado + dia

def dias_sin_hablar(dia_actual, mes_actual, dia_ultima, mes_ultima):
    """
    (uso de operadores, uso de condicionales, uso de funciones)
    recibe: dia_actual, mes_actual, dia_ultima, mes_ultima (enteros)
    resta el dia del anio de la ultima interaccion al dia del anio de hoy;
    si el resultado es negativo, la interaccion fue el anio pasado y se
    le suma 365
    devuelve: dias transcurridos, (entero)
    """
    hoy = dia_del_year(dia_actual, mes_actual)
    ultima = dia_del_year(dia_ultima, mes_ultima)
    transcurridos = hoy - ultima
    if transcurridos < 0:
        transcurridos = transcurridos + DIAS_DEL_ANIO
    return transcurridos

def dias_para_cumple(dia_actual, mes_actual, dia_cumple, mes_cumple):
    """
    (uso de operadores, uso de condicionales, uso de funciones)
    recibe: dia_actual, mes_actual, dia_cumple, mes_cumple (enteros)
    resta el dia del anio de hoy al dia del anio del cumpleanios;
    si el resultado es negativo, el cumpleanios ya paso y se cuenta el
    del anio siguiente sumando 365
    devuelve: dias que faltan para el cumpleanios, (entero)
    """
    hoy = dia_del_year(dia_actual, mes_actual)
    cumple = dia_del_year(dia_cumple, mes_cumple)
    faltan = cumple - hoy
    if faltan < 0:
        faltan = faltan + DIAS_DEL_ANIO
    return faltan

def es_cumple_este_mes(mes_cumple, mes_actual):
    """
    (uso de operadores de comparación, uso de funciones)
    recibe: mes_cumple (valor numerico), mes_actual (valor numerico)
    compara si el mes del cumpleanios es igual al mes actual
    devuelve: True si coinciden, False si no (booleano)
    """
    return mes_cumple == mes_actual

def mensaje_conexion(nombre, dias_transcurridos):
    """
    (uso de condicionales, uso de funciones)
    recibe: nombre (string), dias_transcurridos (valor numerico)
    si pasaron más de 15 días sin hablar, pone un aviso con los dias;
    si no, pone un mensaje de que la conexión está al día
    devuelve: mensaje, (string)
    """
    if dias_transcurridos > LIMITE_DE_DIAS_SIN_HABLAR:
        return ("Hace " + str(dias_transcurridos)
                + " dias que no hablas con " + nombre)
    else:
        return "Conexion al dia con " + nombre

# --------------- Programa Principal ---------------
print("Dia de hoy (numero):")
dia_actual = int(input())
print("Mes de hoy (numero):")
mes_actual = int(input())

print("Nombre del amigo:")
nombre = input()
print("Dia de su birthday (numero):")
dia_cumple = int(input())
print("Mes de su birthday (numero):")
mes_cumple = int(input())
print("Dia de tu ultima interaccion (numero):")
dia_ultima = int(input())
print("Mes de tu ultima interaccion (numero):")
mes_ultima = int(input())

transcurridos = dias_sin_hablar(dia_actual, mes_actual, dia_ultima, mes_ultima)
faltan = dias_para_cumple(dia_actual, mes_actual, dia_cumple, mes_cumple)
mismo_mes = es_cumple_este_mes(mes_cumple, mes_actual)

print(mensaje_conexion(nombre, transcurridos))
print("Dias para su birthday:", faltan)
print("Cumple este mes:", mismo_mes)

print("\n=== MENU PRINCIPAL ===")
print("1. Resumen de conexion")
print("2. Ver si cumple este mes")
print("7. Salir")
print("Elige una opcion:")
opcion = int(input())

if opcion == 1:
    print(mensaje_conexion(nombre, transcurridos))
elif opcion == 2:
    if mismo_mes:
        print(nombre + " cumple este mes")
    else:
        print(nombre + " no cumple este mes")
elif opcion == 7:
    print("Saliendo")
else:
    print("Opcion no valida")
